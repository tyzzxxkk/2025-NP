// 1. Exam 클래스 설계하기
// 프로그래밍 수업에서는 시험 점수를 관리하는 Exam 클래스 구현
// 생성자에 아무 값이 없으면 0, 있으면 해당 값으로 score 반영
// 점수를 외부에서 임의로 수정하지 못하도록 private 필드를 사용
// 변수명 : score
// 기본값 : 0
// 점수는 get ,set 키워드를 사용하여 변경 및 출력 가능
// 0 ~ 100 사이의 값만 변수 저장 가능(이외는 아무 일도 일어나지 않음)
// 점수를 확인할 때는 합격 / 불합격 여부가 자동으로 출력되도록 구현
// 점수가 60점 이상이면 "합격 : [점수]"
// 점수가 60점 미만이면 "불합격"

class Exam {
    // 🔒 private 필드
    #score = 0;
  
    constructor(score = 0) {
      this.score = score; // setter를 통해 유효성 검사
    }
  
    // ✅ getter — 점수 확인 시 자동으로 합격/불합격 표시
    get score() {
      if (this.#score >= 60) {
        console.log(`합격 : ${this.#score}`);
      } else {
        console.log("불합격");
      }
      return this.#score;
    }
  
    // ✅ setter — 0~100 범위만 허용
    set score(value) {
      if (typeof value === "number" && value >= 0 && value <= 100) {
        this.#score = value;
      }
      // 유효하지 않으면 아무 일도 일어나지 않음
    }
  }
  
  // ----------------------
  // ✅ 테스트 코드
  // ----------------------
  const exam1 = new Exam();
  console.log(exam1.score); // 불합격
  exam1.score = 85;
  console.log(exam1.score); // 합격 : 85
  
  const exam2 = new Exam(90);
  console.log(exam2.score); // 합격 : 90
  exam2.score = 40;
  exam2.score = 150; // 무시됨
  console.log(exam2.score); // 불합격  